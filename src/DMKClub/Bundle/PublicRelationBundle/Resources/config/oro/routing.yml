dmkclub_pr_bundle:
    resource:     "@DMKClubPublicRelationBundle/Controller"
    type:         annotation
    prefix:       /dmkclub


dmkclub_pr_bundle_api:
    resource: "@DMKClubPublicRelationBundle/Resources/config/oro/routing_api.yml"
    type: rest
    prefix:       api/rest/{version}
    requirements:
        version:  latest|v1
    defaults:
        version:  latest
